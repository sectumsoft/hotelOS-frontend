export const environment = {
  production: true,
  // Placeholder — production hosting isn't provisioned yet (see PROJECT.md).
  // Point this at the real API host once it exists.
  apiUrl: 'https://api.innwise.com/api',
  appName: 'INNWISE'
};
